// Global setup for Slidev
import "../styles.css";
import { defineAppSetup } from "@slidev/types";

// Provide an empty setup so Slidev can register the file without build errors
export default defineAppSetup(() => {});
